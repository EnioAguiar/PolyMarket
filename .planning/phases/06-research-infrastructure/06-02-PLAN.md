# Railway Volume Documentation

**Reference:**
- Railway Volumes: https://docs.railway.app/persistence/volumes
- Railway PostgreSQL (optional alternative): https://docs.railway.app/databases/postgresql

---

---
phase: 06-research-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - railway.json
  - docker-compose.yml
  - src/db/index.ts
  - config.yaml
autonomous: true
requirements:
  - DEPL-07

must_haves:
  truths:
    - "Railway volume mounted at /data"
    - "SQLite database persists across deploys"
    - "Database path configurable via environment"
  artifacts:
    - path: "railway.json"
      provides: "Railway volume configuration"
    - path: "docker-compose.yml"
      provides: "Local development with volume"
    - path: "src/db/index.ts"
      provides: "Database connection with volume path"

---

<objective>
Set up Railway volume for persistent storage. Bot data persists across deploys.
</objective>

<context>
@.planning/phases/06-research-infrastructure/06-CONTEXT.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Railway volume in railway.json</name>
  <files>
    - railway.json
  </files>
  <action>
Update railway.json to include volume configuration:

Reference: https://docs.railway.app/persistence/volumes

Add volume mount at /data for SQLite persistence.
  </action>
  <verify>grep "/data" railway.json</verify>
  <done>Railway volume configured at /data</done>
</task>

<task type="auto">
  <name>Task 2: Update database path configuration</name>
  <files>
    - src/db/index.ts
    - config.yaml
  </files>
  <action>
Update database connection to use /data volume:

```typescript
const DB_PATH = process.env.DB_PATH || '/data/polymarket.db';
```

Also create docker-compose.yml for local development with volume.
  </action>
  <verify>grep "/data" src/db/index.ts</verify>
  <done>Database path configured for volume persistence</done>
</task>

<task type="auto">
  <name>Task 3: Create docker-compose for local dev</name>
  <files>
    - docker-compose.yml
  </files>
  <action>
Create docker-compose.yml with volume for local development:

```yaml
services:
  bot:
    build: .
    volumes:
      - polymarket-data:/data
    env_file:
      - .env

volumes:
  polymarket-data:
```

This matches Railway volume behavior for local testing.
  </action>
  <verify>grep "polymarket-data" docker-compose.yml</verify>
  <done>Local development with volume configured</done>
</task>

</tasks>

<success_criteria>
- Railway volume mounted at /data
- SQLite database persists across deploys
- docker-compose.yml for local development
</success_criteria>

<output>
After completion, create `.planning/phases/06-research-infrastructure/06-02-SUMMARY.md`
</output>
