---
phase: quick
plan: 01
type: execute
subsystem: research
autonomous: true

must_haves:
  truths:
    - "NewsData replaces Google adapter (already exists at src/research/newsdata.ts)"
    - "Crawl4AI uses search-then-scrape approach (already exists at src/research/crawl4ai_search.ts)"
    - "Wrong files in src/research/sources/ are removed"
  artifacts:
    - path: "src/research/sources/google.ts"
      action: "DELETE - NewsData already exists"
    - path: "src/research/sources/crawl4ai.ts"
      action: "DELETE - using wrong approach with hardcoded URLs"
---

# Fix Research Sources

**Task:** Remove wrong Google adapter and fix Crawl4AI to use search-first approach

## Context
- `src/research/newsdata.ts` exists and works correctly (NewsData.io API)
- `src/research/crawl4ai_search.ts` + `scripts/crawl4ai_search.py` exists and does search first, then scrape
- `src/research/sources/google.ts` was wrongly created (should use NewsData)
- `src/research/sources/crawl4ai.ts` was wrongly created (uses hardcoded URL mapping)

## Actions

### Task 1: Delete wrong files
- DELETE `src/research/sources/google.ts`
- DELETE `src/research/sources/crawl4ai.ts`

### Task 2: Verify remaining files
- Verify `src/research/newsdata.ts` exists and exports NewsDataAdapter
- Verify `src/research/crawl4ai_search.ts` exists and exports Crawl4AISearchAdapter
- Verify `scripts/crawl4ai_search.py` exists and does search-first approach
